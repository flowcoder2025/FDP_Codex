# FDP_Codex Fix Plan

Status: live backlog.

Reconciliation authority: `docs/decisions/2026-07-10-control-plane-operational-integrity.md` and `docs/records/validation-wi-cx0062-fix.md`.

Independent verification authority: `docs/decisions/2026-07-10-independent-blind-adversarial-review-gate.md` and `docs/specifications/independent-review-evidence.md`.

Authority: `docs/decisions/2026-07-08-fdp-codex-operating-foundation.md`, `docs/decisions/2026-07-08-repository-license-binding.md`, `docs/decisions/2026-07-08-bootstrap-publication-envelope.md`, `docs/decisions/2026-07-08-public-readiness-boundary.md`, `docs/decisions/2026-07-08-evaluation-surface-baseline.md`, `docs/decisions/2026-07-08-context-pack-command-surface.md`, `docs/decisions/2026-07-08-context-selection-rule-table.md`, `docs/decisions/2026-07-08-decision-queue-state-codes.md`, `docs/decisions/2026-07-08-ki-identity-severity-policy.md`, `docs/decisions/2026-07-08-handoff-size-policy.md`, `docs/decisions/2026-07-08-autonomy-default-options-packet.md`, `docs/decisions/2026-07-08-operating-policy-lock.md`, `docs/decisions/2026-07-08-session-boundary-automation-contract.md`, `docs/decisions/2026-07-08-tooling-typescript-baseline.md`, `docs/decisions/2026-07-08-tooling-strictness-probe.md`, `docs/decisions/2026-07-08-automation-run-surface-installation.md`, `docs/decisions/2026-07-08-context-ledger-dedupe-policy.md`, `docs/decisions/2026-07-08-layer-2-chunk-id-scope-policy.md`, `docs/decisions/2026-07-08-flow-state-readable-snapshot.md`, `docs/decisions/2026-07-08-a2-worktree-isolation-repair-gate.md`, `docs/decisions/2026-07-10-runtime-snapshot-state-reconciliation.md`, `docs/decisions/2026-07-10-layer-2-scope-code-accepted.md`, `docs/decisions/2026-07-10-ephemeral-worker-controller-boundary.md`, `docs/decisions/2026-07-10-context-selection-breadth-guard.md`, `docs/records/validation-wi-cx0055-feat.md`, `docs/records/validation-wi-cx0057-docs.md`, `docs/records/validation-wi-cx0058-fix.md`, `.flowset/state.json`, `.flowset/runtime-snapshot.json`, `docs/specifications/runtime-snapshot.md`, `docs/specifications/a2-handoff-receiver-contract.md`, `docs/records/layer-2-scope-code-options-2026-07-08.md`, `docs/records/layer-2-scope-code-decision-handback-2026-07-08.md`, `docs/records/session-orchestration-control-plane-audit-2026-07-08.md`, `docs/records/validation-wi-cx0048-test.md`, `docs/records/validation-wi-cx0049-docs.md`, `docs/records/validation-wi-cx0050-test.md`, `docs/records/validation-wi-cx0051-test.md`, `docs/records/validation-wi-cx0052-test.md`, `docs/records/validation-wi-cx0053-docs.md`, `docs/records/validation-wi-cx0054-fix.md`, `docs/records/validation-wi-cx0038-docs.md`, `docs/specifications/layer-2-knowledge-scaffold.md`, `docs/policies/decision-queue.md`, `docs/manifest.yaml`, and current policy docs.

Discipline:

- Use one focused WI at a time unless an approval envelope explicitly allows batching.
- Use category-bearing WI ids: `WI-CXNNNN-category`.
- Do not store completed task detail here.
- Completed evidence belongs in `docs/records/`, `docs/decisions`, git history, PRs, or `.flowset/handoff.md` summaries.
- Keep this file compact and live.
- Triage must choose both the next WI and the evaluator strategy codes.
- Non-trivial work proceeds through the branch-first lifecycle in `docs/policies/git-workflow.md`.
- External Issues and PRs require triage and approval under `docs/policies/github-issue-governance.md` before implementation starts.
- Decision Needed items use `docs/policies/decision-queue.md` state codes.
- Every non-trivial R1/R2/R3 WI requires a clean-context independent adversarial review of the current PR head before `pr:approved-merge`.

## Current Priority

- [ ] WI-CX0060-test Trusted Ephemeral Worker End-to-End Proof: land a truthful `blocked-external` evidence checkpoint, not a completed proof, through draft PR #65 only after the exact final head passes canonical local validation, fresh blind adversarial review, required status, and live control-plane audit. Preserve Issues #55 and #61 as open and do not claim the externally blocked end-to-end proof complete.
- Latest gate: reviewer 019f5b52-28c5-73b2-8eb2-cb03c9325e75 returned FAIL in review 4684593492 on exact head c6c7952fdbab2c6172aa2e1b2b8b3924985e2b6e with P1 target-ancestor/arbitrary command substitution and P2 malformed candidate metadata plus Windows CI nondeterminism. Issues #66, #69, and #70 are open for same-WI repayment before another review.
  - Earlier review history includes reviewer 019f5a78-d8f4-73e0-8744-2f8f070810af and reviewer 019f5b27-a680-7d20-8c02-bb5c71eb6bc6 as evidence only; every remediation commit invalidates prior PASS or FAIL generations and requires a new clean-context exact-head verdict.

## Triggered Work

- None. The hourly worktree runner is retired; its historical first-output trigger must not reactivate or recreate it.

## Next Candidates

- WI-CX0064-docs Local v0.1 Self-Hosting Completion Contract: after WI-CX0060 lands, map the remaining product-critical gaps and sequence local self-hosted implementation to a v0.1 completion candidate. The provider block limits live model-worker claims but does not freeze local harness development.
- Clean-project portability proof: run only after the local v0.1 candidate is complete; use a newly generated target that includes the instruction-only managed-worker boundary policy, explicitly installs no project-local worker command rule, and does not retrofit the stale dogfood target as the primary proof.

## Known Issue Debt

| Item | Severity | Owner | Trigger | Defer reason | Repayment condition | Hard stop | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| KI-CX-WORKER-004 / Issue #64 Windows process-tree completeness. | High | CODEX | Managed lifecycle could execute a worker after timeout or interruption already won before spawn, orphan wrapper and worker after controller death, bind the watchdog to a reused controller PID before handle acquisition, verify cleanup from exit state without the actual wrapper close event, claim timeout/interruption exit classification without a real CLI process regression, leak or misclassify processes, crash on pending stdin writes, preserve false smoke state, let a terminal-event callback block past the deadline or abort after lifecycle completion, adopt a reused wrapper PID including under the same supervisor, signal an unrelated process after PID reuse between classification and termination, accept worker-forged root, assignment, atomic-child, drain, or error markers, match a known-start identity by name when current start-time metadata is missing, adopt a newly discovered descendant without start-time metadata, close Job and controller handles while the watchdog still waits, or launch a runtime compiler or console host before the Job boundary exists. | Pre-spawn guard race and final check with no wrapper/worker execution, controller pre-acquisition death with no worker side effect, behavioral control-environment isolation, atomic Job containment, exact wrapper-handle termination with PID-only descendant signaling disabled, per-invocation authenticated control markers plus a controller PID-and-creation-FILETIME identity check before exact-handle watchdog acquisition, removal of token and both controller identity fields before worker creation, required actual exact-wrapper close-event evidence, real CLI child-process 124/130 regressions, immutable native root and atomic-child markers, bounded guards and observation, three-state identity classification, startless-descendant rejection, event and stdin failure handling, corrected smoke state, terminal-result callback isolation, exactly-once CLI publication, cancellation-event plus joined watchdog teardown, and a tracked reproducibly built and hash-locked prebuilt WindowsApplication runner with no runtime compiler, PowerShell wrapper, or console-host child pass locally; fresh review, PR validation, merge, and post-merge audit remain. | The explicit deterministic build reproduces the exact checked executable from the tracked source and compiler/reference receipt, and the visible controller verifies the tracked C# source and prebuilt executable hashes before spawn and fails with `runner_artifact_failed` on mismatch; runtime setup creates no temporally valid compiler, PowerShell wrapper, or console-host child after stale-parent creation-time filtering; stalled pre-run setup creates no worker side effect, closes the exact wrapper within the finite bound, and classifies it exactly once; a pre-spawn timeout or interruption creates no wrapper or worker; controller death before watchdog acquisition creates no worker side effect; the worker inherits no control token or controller identity field; every tracked identity is classified; exact wrapper-handle stop and Job-handle close exclusively own termination; no observed descendant receives a PID-only signal; initial root identity requires a native start-time marker whose PID equals the exact spawned wrapper and never uses parent/name fallback; every control marker requires the exact per-invocation token and forged markers cannot change containment state or registered identities; controller PID plus creation FILETIME must match before the exact handle is accepted and handle death terminates the Job; the actual wrapper close event is mandatory before cleanup verification; real CLI child-process tests prove timeout and interruption retain exits 124 and 130 under cleanup failure; known-start identities with missing current start-time metadata remain unknown and unverified; newly discovered descendants require start-time metadata or remain unobserved unknown candidates with verification failed; callback and stdin failures return structured failure; arbitrary callbacks never receive the terminal result and exactly one CLI result is published; normal teardown signals watchdog cancellation and joins it before Job or controller handles close; deterministic cases and smoke evidence pass; PR merges; post-merge audit passes; Issue #64 closes. | Before post-merge WI closeout or generalized managed-worker use. | repaid-on-merge |
| KI-CX-WORKER-005 / Issue #66 Trusted absolute Codex command resolution. | High | CODEX | Exact head a76f111 failed review 4683739642 because relative override or fallback could execute a target-cwd `codex.exe` before sandbox entry, and native failures lost Win32 details. | Nearest-Git-root exclusion, non-overlapping controller-owned trust roots, canonical junction rejection, native operation/code/message regressions, and three consecutive canonical CI runs pass locally; fresh review, merge, and post-merge audit remain. | The final exact head passes command-resolution and native-error regressions, canonical CI, blind review, merge, and post-merge audit; Issue #66 closes. | Before PR readiness, independent-pass or merge-approval labels, merge, or generalized managed-worker use. | repaid-on-merge |
| KI-CX-WORKER-006 / Issue #67 Prompt-boundary terminal schema. | High | CODEX | Exact head a76f111 failed review 4683739642 because prompt timeout and interruption omitted `controller_watchdog_stopped`. | Both paths now emit explicit false watchdog armed/stopped fields and exact containment-shape tests pass locally; canonical validation, fresh review, merge, and post-merge audit remain. | The final exact head passes complete prompt-result schema regressions, canonical CI, blind review, merge, and post-merge audit; Issue #67 closes. | Before PR readiness, independent-pass or merge-approval labels, merge, or schema-complete evidence claims. | repaid-on-merge |
| KI-CX-WORKER-007 / Issue #68 Setup-failure terminal result. | High | CODEX | Exact head c2a24c8 failed review 4684212043 because relative CLI resolution failure emitted only `worker.wrapper_error`. | Shared null-root `setup_failed` publication and relative/missing override CLI regressions pass locally; canonical validation, fresh review, merge, and post-merge audit remain. | Each setup failure publishes exactly one complete null-root `worker.result`; canonical CI, blind review, merge, and post-merge audit pass; Issue #68 closes. | Before PR readiness, independent-pass or merge-approval labels, merge, or exactly-once setup-result claims. | repaid-on-merge |
| KI-CX-CONTROL-002 / Issue #69 Multiline candidate-field live-head audit. | High | CODEX | Exact head c2a24c8 failed review 4684212043 because a continuation-line candidate SHA evaded structured-field parsing. | Indented, lazy, plain, and malformed candidate parsing plus current, stale, abbreviated, and ambiguous regressions pass locally; fresh review, merge, and post-merge audit remain. | Candidate continuation lines are parsed and stale or abbreviated references fail closed against the live PR head; canonical CI, blind review, merge, and post-merge audit pass; Issue #69 closes. | Before PR readiness, independent-pass or merge-approval labels, merge, or false-green-resistant audit claims. | repaid-on-merge |
| KI-CX-WORKER-008 / Issue #70 Canonical Windows worker verification repeatability. | High | CODEX | Exact head c6c7952 failed review 4684593492 because consecutive canonical Windows CI runs failed through different load-sensitive normal-completion paths. | Runtime behavior failed closed and the final census was clean; bounded natural Job convergence, transient observation retry, focused lifecycle validation, and three consecutive full CI runs pass locally; fresh review, merge, and post-merge audit remain. | Natural Job accounting converges within a bound, transient process-table failures retry within an absolute deadline, persistent uncertainty fails closed, consecutive full suites pass, the exact-head review passes, PR #65 merges, post-merge audit passes, and Issue #70 closes. | Before PR readiness, independent-pass or merge-approval labels, merge, or repeatable canonical-verification claims. | repaid-on-merge |
| KI-CX-PROVIDER-001 / Issue #55 Repository-backed model worker trust boundary. | Medium | H1 / execution platform | The public-repository preflight passed, but the exact post-fix local dogfood retry was policy-rejected even after current explicit user approval; the configured external destination is not established as trusted. | WI-CX0059-fix Ephemeral Worker Process Lifecycle Guard repaid process cleanup, and WI-CX0060 preserves the public preflight, failed dogfood run, qualified historical cleanup report, later deterministic Job containment evidence, and truthful command-boundary limitation without bypassing disclosure policy. | The execution platform establishes a trusted model destination and permits the managed repository-backed dogfood proof. | Before completing WI-CX0060-test, dogfood continuation, generalized unattended model worker use, or runner reactivation. | open |
| KI-CX-WORKER-003 / Issue #61 Managed worker final-result and command-boundary proof remain incomplete. | High | CODEX | The first dogfood run timed out after unsupported collaboration, and a later project-local deny rule blocked the visible controller because trusted project rules load for every task. | Built-in fan-out disabling, controller-owned validation, Windows Job Object containment, and unsupported-platform fail-closed behavior pass; no active project rule remains, and command re-entry is explicitly unenforced. | Provide a worker-only command boundary that does not restrict the controller, then complete a repository-backed read-only dogfood run with final output, controller validation, and verified zero residuals. | Before completing WI-CX0060-test as an end-to-end proof, dogfood continuation beyond diagnosis, generalized unattended workers, or runner replacement. | open |
| KI-CX-DOGFOOD-002 / Issue #62 Generated target stale-handoff false green. | High | CODEX | The clean committed target still instructed the controller to review and commit, while both target validators passed. | The finding was discovered read-only and Layer 1 must not silently rewrite Layer 2 state. | The generated target workflow records truthful post-commit state, its validator rejects the stale pattern, the target carries qualified fix evidence, and a clean worker confirms it. | Before claiming target operating state is current, starting another target WI, or generalizing target handoff correctness. | open |
| KI-CX-REVIEW-002 / Issue #63 Current-head independent verdict is invalidated after PR head change. | High | H1 / execution platform | Reviewer 019f5b52-28c5-73b2-8eb2-cb03c9325e75 returned FAIL in GitHub review 4684593492 with one P1 and two P2 findings on exact head c6c7952fdbab2c6172aa2e1b2b8b3924985e2b6e. | Tracked files cannot truthfully self-reference their own resulting commit SHA. Before every review, query `git rev-parse HEAD`, require a clean worktree, compare that live value with Issue #63, and read required checks live; defer and repayment fields must not store a literal candidate SHA. | A clean-context reviewer completes every required command, returns PASS on exact live HEAD with no unresolved P0, P1, or P2, and the PR audit accepts that exact final head. | Before PR readiness, independent-pass or merge-approval labels, or merge. | open |
| KI-CX-REVIEW-001 / Issue #59 Independent reviewer provenance boundary. | High | H1 / execution platform | The repository can validate a controller-attested receipt but cannot verify a signed `multi_agent_v1` reviewer identity. | The current supervised WI uses an actual clean-context agent, live controller inspection, current-head GitHub review evidence, and active user approval without claiming cryptographic provenance. | The execution surface supplies a signed orchestrator attestation or a distinct independently authenticated reviewer identity that the repository can verify. | Before unattended/generalized automated merge, A2/A3 authority expansion, release candidate, public release, or OSS submission. | open |
| KI-CX-STATUS-001 / Issue #60 Trusted workflow identity boundary. | High | H1 / GitHub plan or dedicated publisher | GitHub Free required checks can bind the context to the shared Actions app but not to one workflow file or event. | Supervised merge requires the exact expected Actions run, exact-head independent review, live audit, and active user approval; the status is defense in depth only. | Enable an organization/enterprise required-workflow ruleset or install a dedicated authenticated publisher identity unavailable to candidate workflows. | Before unattended/generalized automated merge, A2/A3 authority expansion, release candidate, public release, or OSS submission. | open |

## Decision Needed Queue

| Item | State | Owner gate | Lock blocker | Repayment trigger |
| --- | --- | --- | --- | --- |
| Release category policy. | DQ-EXTERNAL | H1 | no | Before release candidate, tag, package publication, deployment, or OSS submission. |
| A3 publication/merge envelope beyond the current bootstrap approval scope. | DQ-USER | USER | conditional | Before A3 publication, release, deployment, package publication, OSS submission, or generalized automerge behavior. |
| Whether the validator should adopt a strict YAML parser later. | DQ-DEBT | CODEX | no | When manifest grammar exceeds the current parser or parser drift is observed. |
| Strict TypeScript source conversion or strict-mode tightening. | DQ-DEBT | CODEX | no | Probe installed by WI-CX0040; repay before release-candidate tooling lock or when probe counts are small enough for a focused strictness WI. |
